{
  "name": "puppeteer-screenshot",
  "version": "1.1.0",
  "description": "Take screenshot of a website",
  "private": true,
  "license": "MIT",
  "dependencies": {
    "chrome-aws-lambda": "5.2.1",
    "puppeteer-core": "5.2.1"
  },
  "scripts": {
    "dev": "vercel dev",
    "setup": "yarn global add vercel && vercel login && vercel dev"
  }
}
